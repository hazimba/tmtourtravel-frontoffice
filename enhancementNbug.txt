feature
1. add blog page in /admin/blog and /blog/[id]
2. add mice page
3. add marquee page in /admin/imageSlider

enhancement
1. instead store /package/uuid <SelectItem key={option.value} value={`/package/${option.value}`}>, just store uuid only and set imageSlider.buttonpath to imageSlider.package_id as foreign key to package
2. the language is mixing, bahasa and english
3. enhance categories section
4. minimize the "use client" pages


bug
1.